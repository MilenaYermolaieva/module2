#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main()
{
    string R = get_string("Введіть рядок: ");
    for (int i = strlen(R); i >= 0; i--)
    {
         printf("%c", R[i]);
    }
}
